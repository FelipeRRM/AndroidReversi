<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/dialog_chegando</item>
        <item name="android:windowExitAnimation">@anim/dialog_saindo</item>
    </style>

    <style name="full_dialog"  parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/DialogAnim</item>
    </style>

</resources>
